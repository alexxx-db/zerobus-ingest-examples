# Build tools for the native C row encoder (python3.12-slim doesn't include them)
setuptools>=68
wheel

# Locust + gevent-compatible gRPC
locust>=2.44,<3
grpcio>=1.60
gevent>=23.7

# Zerobus Python SDK (Rust-backed)
databricks-zerobus-ingest-sdk>=1.2

# Arrow / parquet for streaming the NEOWISE-R dataset
pyarrow>=14.0,<20.0
numpy>=1.24

# S3 access to the public NASA/IRSA NEOWISE bucket (unsigned)
boto3>=1.28

# Required by the SDK for the OAuth token exchange
requests>=2.28,<3

# protobuf — pinned to the SDK's range to avoid descriptor incompatibilities
protobuf>=4.25.0,<7.0
